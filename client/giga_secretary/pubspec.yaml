name: giga_secretary
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  just_audio: ^0.9.36
  audio_session: ^0.1.14
  file_picker: ^8.1.2
  googleapis: ^10.0.0
  googleapis_auth: ^1.4.0
  google_sign_in: ^5.0.7
  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/file1.mp3
